{
  "name": "puppeteer-scraper",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node --enable-source-maps --import ./src/index.ts",
    "sandbox": "node --enable-source-maps --import ./src/sandbox.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "dotenv": "^16.5.0",
    "puppeteer": "^24.9.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "prettier": "^3.5.3"
  }
}
